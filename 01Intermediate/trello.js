// const days = ['Mon', 'Tue', 'Wed', 'Thr', 'Fri', 'Sat']
// days.forEach(function (day, index) {
//     console.log(`stats with ${index + 1} -- ${day}`);
// })

// const months = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'July', 'Aug', 'Sept', 'Oct', 'Nov', 'Dec']
// months.forEach(function (month, number) {
//     console.log(`${number + 1}  ----- ${month}`);
// })

// const days = ['Mon', 'Tue', 'Wed', 'Thr', 'Fri', 'Sat']
// //console.log(days.length - 1);
// for (let index = days.length - 1; index >= 0; index--) {
//     console.log(days[index]);

// }



const myTodos = []

myTodos.push('Buy Bread')
myTodos.push('Record videos for youtube')
myTodos.push('Go toGym')

myTodos.forEach(function (todo, index) {

    console.log(`Your task no. ${index + 1} is: ${todo}`);

})


// const myTodo = []

// myTodo.push('Buy Bread')
// myTodo.push('Record videos for youtube')
// myTodo.push('Go to Gym')


// for (let i = myTodo.length - 1, j = 0; i >= 0; i-- , j++) {
//     const work = myTodo[i];
//     console.log(`Your task no. ${j + 1} is: ${work}`);
// }